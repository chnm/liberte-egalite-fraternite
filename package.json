{
  "name": "liberte-egalite-fraternite",
  "version": "0.0.0",
  "private": true,
  "description": "Build toolchain (Pagefind search index) for the revolution.chnm.org -> Hugo static site.",
  "scripts": {
    "index": "pagefind --site public"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
